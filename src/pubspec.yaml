name: workspace
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  google_fonts: ^5.1.0
  flutter:
    sdk: flutter
  logger: ^1.1.0
  flutter_riverpod: ^2.3.7
  go_router: ^10.1.0
  flutter_web_plugins:
    sdk: flutter
  percent_indicator: ^4.2.3
  freezed_annotation: ^2.4.1
  # device_preview: ^1.1.0 # 開発停止中
  device_preview_minus: ^1.0.0 # こっちを使う
  cupertino_icons: ^1.0.2

dev_dependencies:
  freezed: ^2.4.2
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.1

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/
    - assets/google_fonts/
  uses-material-design: true
